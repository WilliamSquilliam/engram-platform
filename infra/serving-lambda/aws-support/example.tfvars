# Copy to a real *.tfvars (gitignored) and adjust — an empty file applies the
# defaults (a fresh versioned cart bucket + the bucket-scoped serving IAM user).

# region           = "us-east-1"
# cart_bucket_name = ""                 # empty = engram-carts-lambda-<acct>-us-east-1
# cart_store_prefix = "cartridges"
